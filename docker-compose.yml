services:
  litreview:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - GROQ_API_KEY_WRITER=${GROQ_API_KEY_WRITER}
      - PORT=8000
    restart: unless-stopped
